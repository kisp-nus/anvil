
chan ch {
    left req : (logic[8]@#1) @dyn - @#2
}

proc syn2_example_receiver (endp : left ch) {
    loop {
        cycle 2 >>
        let r = recv endp.req >>
        dprint "Value = %d" (r)
    }
}

