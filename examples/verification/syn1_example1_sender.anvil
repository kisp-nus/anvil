chan ch {
    left req: (logic[8]@#2) @#3 - @dyn
}

proc syn1_example1_sender(ep: right ch) {
    reg r : logic[8];
    loop {
        cycle 2 >>
        set r := *r + 8'b1 >>
        dprint "Data Send = %d" (*r);
        send ep.req (*r)
    }
}
