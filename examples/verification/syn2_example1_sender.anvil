
chan ch {
    left req : (logic[8]@#2) @dyn - @#3
}

proc syn2_example1_sender (endp : right ch) {
    reg r : logic[8];
    loop {
        cycle 1 >>
        set r := *r + 8'b1 >>
        dprint "Data Send = %d" (*r);
        send endp.req (*r)
    }
}
