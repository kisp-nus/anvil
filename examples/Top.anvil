type bit = logic[1];
chan ch{
    left empty : (bit@#1) @#1 - @#1
}
proc Foo(ep[3] : left ch){
    loop{
        let x =  recv ep[0].empty >>
        dprint "Received: %d" (x) >>
        cycle 1
        // Do something with data
    }

}
