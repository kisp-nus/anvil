
chan ch {
    left req: (logic[8]@#2) @#1 - @dyn
}

proc extern_example3_sender(ep: right ch) {
    reg r : logic[8];
    loop {
        cycle 2 >>
        set r := *r + 8'b1 >>
        dprint "Data Send = %d" (*r);
        send ep.req (*r)
    }
}
