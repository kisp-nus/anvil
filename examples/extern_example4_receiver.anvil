chan ch {
    left res : (logic[8]@#3) @#1 - @dyn
}

proc extern_example4_receiver (endp : left ch) {
    reg data : logic[8];
    loop {
        let data = recv endp.res >>
        dprint "Data Receive: %d" (data) >>
        cycle 1 
    }
}

